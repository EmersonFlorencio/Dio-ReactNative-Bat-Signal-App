import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  button: {
    alignItems: 'center',
    backgroundColor: '#646464c7',
    padding: 20,
    width: 300,
    borderRadius: 10,
  },
  text: {
    fontSize: 24,
    fontWeight: 'bold'
  }
});

export default styles;